#include <iostream>
// required header for smart pointer
#include <memory>

//! smart pointers
//? wrapper for raw pointers
//? memory is deallocated automatically so memory leak
//? Types: unique pointer, shared pointer and weak pointer

int main()
{
    // unique pointer
    // unique_ptr<int> unPtr1 = make_unique<int>(25);
    return 0;
}
